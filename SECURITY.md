# Security Policy

## 安全使用边界

VULNCLAW 是**仅供授权渗透测试与安全学习**使用的自动化平台。使用本工具前，请确保已获得目标系统所有者的**书面测试授权**。未授权扫描/利用行为由使用者自行承担法律责任。

- 危险模式（`--dangerous`）会实际执行利用，默认关闭，需显式开启。
- 平台不提供任何规避法律、未经授权访问的能力；所有功能均以授权测试为前提。

## 报告漏洞

如果你发现 VULNCLAW 自身的安全漏洞（而非被扫描目标的问题），请**不要公开披露**，按以下方式私密报告：

1. **优先：GitHub 私有安全通告（Private Security Advisory，推荐）**
   - 打开仓库主页 → `Security` → `Advisories` → `New draft security advisory`
   - 这种方式支持加密沟通、可协同修复、并在修复前完全不对外暴露漏洞细节。

2. **备选：向项目维护者发送加密邮件**（当仓库未启用 GitHub Advisories 时使用）
   - **当前维护者安全邮箱**：`vulnclaw.official@outlook.com`。
   - 若使用 PGP/GPG，建议同步把公钥指纹附在 `SECURITY.md` 下方，或通过 GitHub 主页披露。

> 发布前自检清单（确保开源后不会出现无效联系方式）：
> - [x] `SECURITY.md` 中的安全邮箱 `vulnclaw.official@outlook.com` 已替换为真实维护者邮箱。
> - [ ] GitHub `Security` → `Advisories` 已开启（强烈推荐）。
> - [ ] `.env` 中的真实 API Key / Token 不会随仓库推送（已在 `.gitignore` + §4 免责声明中覆盖，这里再核对一次）。

3. 邮件 / 安全通告中请包含：
   - 影响版本（`vulnclaw/__init__.py` 的 `__version__` 或 commit hash）
   - 漏洞类型与严重性评估
   - 复现步骤（尽量最小化）
   - 如已修复，附上补丁建议

## 处理流程

| 阶段 | 时限 |
|---|---|
| 确认收到 | 48 小时内 |
| 漏洞评估 | 7 天内 |
| 修复发布 | 视严重性 14–90 天 |

严重漏洞修复后，会同步更新 `CHANGELOG.md` 并建议所有使用者升级。

## 支持范围

| 版本 | 支持 |
|---|---|
| 最新 release | 完整支持 |
| 历史 release | 仅安全修复（最近 1 个版本） |

## 免责声明

本平台按"原样"提供，不附带任何明示或默示担保。作者不对使用本平台造成的任何直接或间接损害负责。发布前请移除 `.env` 中的真实 API Key 等敏感信息。
